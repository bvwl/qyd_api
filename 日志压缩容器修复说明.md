# 日志压缩容器修复说明

## 问题

日志压缩容器启动时报错：

```
ModuleNotFoundError: No module named 'app.apis'
```

## 原因

原 Dockerfile 中，日志压缩服务只复制了部分文件：

```dockerfile
# ❌ 错误的做法
COPY app/utils/logs.py app/utils/logs.py
COPY app/utils/__init__.py app/utils/__init__.py
COPY app/__init__.py app/__init__.py
COPY start_log_compressor.py .
```

但是 `app/__init__.py` 中导入了 `app.apis`：

```python
from fastapi import APIRouter
from app.apis import apis_router  # ❌ 这里会失败
```

导致导入失败。

## 解决方案

修改 Dockerfile，复制完整的应用代码：

```dockerfile
# ✅ 正确的做法
FROM backend-base as log-compressor

# 复制完整应用代码（避免导入错误）
COPY . .

# 创建日志目录
RUN mkdir -p logs

# 启动命令
CMD ["python", "start_log_compressor.py"]
```

## 重新构建和启动

### 1. 拉取最新代码

```bash
git pull origin main
```

### 2. 重新构建日志压缩服务

```bash
# 只重新构建日志压缩服务
docker compose -f docker-compose.backend.yml build --no-cache log-compressor
```

或者使用完整重建脚本：

```bash
bash force-rebuild-backend.sh
```

### 3. 启动服务

```bash
docker compose -f docker-compose.backend.yml up -d log-compressor
```

### 4. 验证服务

```bash
# 查看服务状态
docker compose -f docker-compose.backend.yml ps log-compressor

# 查看日志（应该没有错误）
docker compose -f docker-compose.backend.yml logs --tail=30 log-compressor
```

## 预期输出

正常启动后，应该看到类似输出：

```
============================================================
日志压缩服务启动中...
============================================================
配置信息:
  压缩间隔: 每 2 小时
  保留天数: 30 天
  日志目录: logs/
启动时执行首次压缩...
============================================================
开始执行日志压缩任务...
执行时间: 2026-02-22 XX:XX:XX
压缩前统计:
  总文件数: X
  总大小: X.XX MB
  已压缩文件: X
...
日志压缩任务完成
============================================================
日志压缩服务已启动
下次执行时间: 2026-02-22 XX:XX:XX
按 Ctrl+C 停止服务
============================================================
```

## 为什么不优化导入？

虽然可以通过修改 `app/__init__.py` 或创建独立的日志工具来避免这个问题，但：

1. **简单性**：复制完整代码最简单，不需要修改现有代码结构
2. **一致性**：所有容器使用相同的代码结构
3. **维护性**：未来添加新功能时不需要考虑依赖问题
4. **镜像大小**：日志压缩服务的镜像大小与主服务相同（~500MB），可接受

## 故障排查

### 问题1：仍然报导入错误

**检查**：
```bash
# 确认已拉取最新代码
git log --oneline -1

# 应该看到：fix: 修复日志压缩容器导入错误
```

**解决**：
```bash
# 强制重新构建
docker compose -f docker-compose.backend.yml build --no-cache log-compressor
docker compose -f docker-compose.backend.yml up -d log-compressor
```

### 问题2：容器一直重启

**检查**：
```bash
docker compose -f docker-compose.backend.yml logs log-compressor
```

**可能原因**：
- 日志目录权限问题
- 环境变量配置错误
- Python 依赖缺失

**解决**：
```bash
# 检查日志目录权限
ls -la logs/

# 重新构建基础镜像
docker compose -f docker-compose.backend.yml build --no-cache backend-base
docker compose -f docker-compose.backend.yml build --no-cache log-compressor
```

### 问题3：服务正常但不压缩日志

**检查**：
```bash
# 查看配置
docker compose -f docker-compose.backend.yml exec log-compressor env | grep LOG
```

**确认配置**：
```bash
LOG_COMPRESS_INTERVAL_HOURS=2
LOG_COMPRESS_ON_STARTUP=1
LOG_RETENTION_DAYS=30
```

## 总结

✅ 已修复日志压缩容器的导入错误  
✅ 使用完整代码复制，避免依赖问题  
✅ 保持代码结构一致性  

现在可以正常使用 `bash force-rebuild-backend.sh` 启动所有服务！
