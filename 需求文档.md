# 公司管理系统

## 技术栈

### 后端

| 技术/组件    | 版本/用途  | 补充说明                                                   |
| ------------ | ---------- | ---------------------------------------------------------- |
| Python       | 3.12       | 核心开发语言，选择3.12版本兼顾性能与新特性                 |
| FastAPI      | 最新稳定版 | 高性能异步Web框架，支持自动生成API文档                     |
| Redis        | 8.4.0      | 缓存热点数据（如用户权限、考勤统计）、分布式锁、接口限流   |
| Tortoise-ORM | 最新稳定版 | 异步ORM框架，适配FastAPI的异步特性，简化数据库操作         |
| Pydantic     | 2.x        | 数据校验、序列化/反序列化，支持模型嵌套和自定义校验规则    |
| MySQL        | 8.0.4      | 主从分离部署：主库负责写操作，从库负责读操作，提升并发能力 |
| Uvicorn      | 最新稳定版 | FastAPI的ASGI服务器，负责运行后端服务                      |
| Alembic      | 最新稳定版 | 数据库迁移工具，管理MySQL表结构变更                        |
| JWT          | PyJWT      | 用户身份认证，生成/校验token                               |
| APScheduler  | 最新稳定版 | 异步任务处理                                               |

### 前端

| 技术/组件     | 版本/用途 | 补充说明                                          |
| ------------- | --------- | ------------------------------------------------- |
| React         | 18.x      | 核心前端框架，使用函数式组件+Hook开发             |
| TypeScript    | 5.x       | 类型校验，提升代码可维护性                        |
| React Router  | 6.x       | 前端路由管理，实现页面跳转                        |
| Ant Design    | 5.x       | 企业级UI组件库，快速搭建管理界面                  |
| Redux Toolkit | 最新版    | 状态管理，统一管理全局数据（如用户信息、权限）    |
| Axios         | 1.x       | 前后端交互，封装请求拦截器（token携带、异常处理） |
| Vite          | 5.x       | 前端构建工具，提升开发/打包效率                   |
| ECharts       | 5.x       | 数据可视化                                        |

### 运维

| 技术/组件      | 用途                                         |
| -------------- | -------------------------------------------- |
| Docker         | 容器化部署，统一开发/测试/生产环境           |
| Docker Compose | 编排多容器服务（后端、前端、Redis、MySQL等） |
| Git            | 代码版本管理                                 |

## 开发规范

### 后端开发规范

- 代码遵循PEP8规范，使用类型注解；

- API接口统一前缀（如`/v1/{模型名称}`）
- 所有api可以通过jwt或者用户token进行访问

- 异常统一处理，避免直接抛出原始错误；

- 数据库操作使用事务，确保数据一致性；

- 敏感数据（如密码）加密存储（推荐bcrypt）。

### 前端开发规范

- 组件化开发，通用组件抽离（如表单、表格、弹窗）；

- 接口请求封装，统一处理token过期、网络异常；

- 路由守卫，未登录/无权限用户跳转到登录页；

- 代码提交前执行ESLint校验，保证代码格式统一。

## 文档结构

```
(zy312) ~/code/python/company/api/qyd_api git:[main]
tree
.
├── backend # 后端
│   ├── Dockerfile # 后端docker
│   ├── README.md #后端文档
│   ├── app
│   │   ├── __init__.py
│   │   ├── apis #api文件夹
│   │   │   ├── __init__.py
│   │   │   ├── deps.py
│   │   │   └── v1 #版本控制
│   │   │       ├── __init__.py
│   │   │       ├── mail
│   │   │       │   ├── __init__.py
│   │   │       │   ├── auth.py
│   │   │       │   ├── info.py
│   │   │       │   └── outlook.py
│   │   │       ├── server
│   │   │       │   ├── __init__.py
│   │   │       │   ├── account.py
│   │   │       │   ├── country.py
│   │   │       │   ├── group.py
│   │   │       │   └── info.py
│   │   │       └── user
│   │   │           ├── __init__.py
│   │   │           └── auth.py
│   │   ├── clients #第三方接口
│   │   │   ├── __init__.py
│   │   │   └── outlook.py
│   │   ├── core #核心配置
│   │   │   ├── __init__.py
│   │   │   └── settings.py
│   │   ├── crud #各模型对应的 CRUD 封装
│   │   │   ├── base.py
│   │   │   ├── mail
│   │   │   │   ├── auth.py
│   │   │   │   └── info.py
│   │   │   ├── server
│   │   │   │   ├── account.py
│   │   │   │   ├── country.py
│   │   │   │   ├── group.py
│   │   │   │   └── info.py
│   │   │   └── user
│   │   │       ├── log.py
│   │   │       ├── permission.py
│   │   │       ├── role.py
│   │   │       ├── route.py
│   │   │       ├── token.py
│   │   │       └── user.py
│   │   ├── logs
│   │   │   ├── __init__.py
│   │   ├── main.py
│   │   ├── models
│   │   │   ├── base.py
│   │   │   ├── mail.py
│   │   │   ├── project.py
│   │   │   ├── server.py
│   │   │   └── user.py
│   │   ├── schemas #Pydantic 请求/响应模型
│   │   │   ├── base.py
│   │   │   ├── mail
│   │   │   │   ├── auth.py
│   │   │   │   ├── info.py
│   │   │   │   └── outlook.py
│   │   │   ├── server
│   │   │   │   ├── account.py
│   │   │   │   ├── country.py
│   │   │   │   ├── group.py
│   │   │   │   └── info.py
│   │   │   └── user
│   │   │       ├── log.py
│   │   │       ├── permission.py
│   │   │       ├── role.py
│   │   │       ├── route.py
│   │   │       ├── token.py
│   │   │       └── user.py
│   │   ├── tests #测试文件夹
│   │   │   ├── __init__.py
│   │   │   └── api_requests_test.py
│   │   └── utils #工具文件夹
│   │       ├── __init__.py
│   │       ├── decorators.py
│   │       ├── exceptions.py
│   │       ├── logs.py
│   │       ├── redis_tool.py
│   │       ├── req.py
│   │       ├── retry.py
│   │       └── time_tool.py
│   ├── compose.yml
│   ├── migrations #模型迁移aerich生成
│   │   └── models
│   │       ├── 0_20260114152920_init.py
│   │       ├── 1_20260114162915_update.py
│   ├── pyproject.toml
│   ├── requirements.txt #三方库
│   ├── scripts #脚本文件
│   │   ├── init_db.sh
│   │   └── update_db.sh
│   └── start.py #启动入口
├── .gitignore #git隐藏文件
└── 需求文档.md #整体文档

```



## 模型定义

#### 基础模型(BaseModel)- 所有模型都会应用基础模型

- id(uuid): ID
- create_time(datetime) : 创建时间 索引
- update_time(datetime): 更新时间

### 服务器模型

#### 服务器国家模型(ServerCountry)

- short_name(str) : 国家简称 索引 唯一
- name(str): 国家名称
- status(int): 状态(枚举) 索引 1:正常 2:异常

#### 服务器分组模型(ServerGroup) - 一对多关联服务器国家模型

- name(str): 服务器名称 为一 索引
- status(int): 状态(枚举) 索引 1:正常 2:异常

#### 服务器信息模型(ServerInfo) - 一对多关联服务器分组模型(可空)

- host(str): 服务器地址 索引
- ssh_port(int): 服务器端口 可空
- password(text): 服务器密码 可空 加密存储
- status(int): 状态(枚举) 1:正常 2:异常
- domain(str): 域名 索引 可空
- is_sale(int): 是否销售(枚举) 1:是 2:否
- port(int): 代理端口

#### 服务器账号模型(ServerAccount) - 一对一关联用户信息模型

- username(str): 用户名 索引
- password(text): 密码 加密存储

### 邮箱类

#### 邮箱信息模型(EmailInfo) - 一对多关联服务器信息模型(可空)

- email(str): 邮箱号 唯一 索引
- password(text): 邮箱密码 加密存储
- auxiliary_email(str): 辅助邮箱
- auxiliary_email_password(text): 辅助邮箱密码 加密存储
- client_id(str):客户端id 可空
- access_token(text): 会话token 可空 加密存储
- refresh_token(text): 刷新token 可空 加密存储
- status(int): 状态(枚举) 1:正常 2:异常

### 用户类

#### 用户角色模型(UserRole) - 多对多关联前端路由模型(可空)

- name(str): 角色名称
- code(str): 角色标识 唯一 索引
- description(str): 角色描述

#### 用户信息模型(UserInfo) - 多对多关联用户角色模型

- email(str): 用户注册邮箱 唯一 索引
- password(text): 用户注册密码 CryptContext加密
- nickname(str): 用户昵称 唯一 索引
- status(int): 用户状态(枚举) 索引 1:正常 2:停用 3:锁定 4:封禁

#### 用户token模型(UserToken) - 一对多关联用户信息模型

- token(str): 用户token 索引 唯一 用户邮箱+13位时间戳+qyd md5生成
- status(int): 状态(枚举) 1:正常 2:异常

#### 用户日志模型(UserLog) - 一对多关联用户信息模型

- action(int): 操作类型(枚举) 索引 1:查询 2:创建 3: 修改 4:删除 5: 其他1 6:其他2
- description(text): 操作描述
- ip(str): ip地址 索引

#### 前端路由模型(FrontendRoute) - 一对多关联用户角色模型

- name(str): 路由名称
- path(str): 路由路径
- component(str): 前端组件路径
- status(int): 状态(枚举) 1:正常 2:异常

### 项目模型

#### 项目信息模型(ProjectInfo) - 多对多关联用户信息模型

- name(str): 项目名称
- status(int): 状态(枚举) 1:正常 2:未编写 3: 编写中 4: 项目结束 5: 项目跑路 6: 项目维护 7: 未分配 8: 账号不支持 9: ip不支持
- content(file): 项目内容 pdf,docx,excel等文件 (仅存文件路径或key)

#### 项目钱包模型(ProjectWallet)

- private_key(text): 私钥 aes加密
-  public_key(text): 公钥
- mnemonic(text): 助记词 aes加密

#### 项目账号模型(ProjectAccount) - 一对多关联项目信息模型 一对多关联服务器信息模型(可空)

- account(text): 账号 索引
- password(text): 密码 可空 加密存储
- status(int): 状态(枚举) 1:正常 2:异常
- account_type(int): 账号类型(枚举) 1:邮箱 2:钱包 3:x 4:其他1 5:其他2
- data(json): 数据 可空

#### 项目余额模型(ProjectBalance) - 一对一关联项目账号模型

- balance(decimal): 余额
- variable(decimal): 变动余额
- history(json): 历史余额(可拆分为独立流水表)

## 需求定义

