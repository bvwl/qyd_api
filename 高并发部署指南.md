# 高并发部署指南

## 快速开始

### 1. 配置环境变量

```bash
# 复制示例配置
cp .env.example .env

# 编辑配置文件，填入实际的数据库和Redis信息
vim .env
```

需要修改的关键配置：
- `DB_PASSWORD`: 数据库密码
- `DB_SLAVE1_PASSWORD`: 从库1密码
- `DB_SLAVE2_PASSWORD`: 从库2密码
- `REDIS_PASSWORD`: Redis密码
- `JWT_SECRET_KEY`: JWT密钥（建议使用随机字符串）

### 2. 启动服务

```bash
bash force-rebuild-backend.sh
```

这会自动启动：
- **Backend API** (4 Workers) - 处理HTTP请求
- **Queue Worker** (16 并发) - 处理队列任务
- **Log Compressor** - 独立日志压缩服务 ✨

### 3. 验证服务

```bash
# 查看所有服务状态
docker compose -f docker-compose.backend.yml ps

# 应该看到3个服务都在运行：
# - qyd-backend-api
# - qyd-queue-worker
# - qyd-log-compressor
```

### 4. 访问服务

- API文档: http://192.168.13.6:6080/docs
- 前端: http://192.168.13.6

## 服务架构

```
┌─────────────────────────────────────────────────────────┐
│                    负载均衡 (可选)                        │
└─────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
┌───────▼────────┐  ┌───────▼────────┐  ┌──────▼──────┐
│  Backend API   │  │  Backend API   │  │ Backend API │
│  (4 Workers)   │  │  (4 Workers)   │  │ (4 Workers) │
└────────────────┘  └────────────────┘  └─────────────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
┌───────▼────────┐  ┌───────▼────────┐  ┌──────▼──────┐
│ Queue Worker   │  │ Queue Worker   │  │Queue Worker │
│ (16 并发)      │  │ (16 并发)      │  │ (16 并发)   │
└────────────────┘  └────────────────┘  └─────────────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            │
                    ┌───────▼────────┐
                    │ Log Compressor │
                    │  (独立服务)     │
                    └────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
┌───────▼────────┐  ┌───────▼────────┐  ┌──────▼──────┐
│  MySQL 主库    │  │  MySQL 从库1   │  │ MySQL 从库2 │
└────────────────┘  └────────────────┘  └─────────────┘
        │
┌───────▼────────┐
│     Redis      │
└────────────────┘
```

## 性能配置

### 当前配置（单机）

```
服务配置:
- Backend API: 1个容器 × 4 Workers = 4个进程
- Queue Worker: 1个容器 × 16 并发 = 16个并发
- Log Compressor: 1个容器（独立运行）

预期性能:
- HTTP QPS: 5,000 - 10,000
- 队列处理: 10,000+ 条/秒
- 平均响应: < 50ms
```

### 扩展配置（多容器）

```bash
# 扩展到5个后端容器 + 5个队列容器
docker compose -f docker-compose.backend.yml up -d \
  --scale backend-api=5 \
  --scale queue-worker=5

# 扩展后性能:
# - Backend API: 5个容器 × 4 Workers = 20个进程
# - Queue Worker: 5个容器 × 16 并发 = 80个并发
# - HTTP QPS: 20,000 - 50,000
# - 队列处理: 50,000+ 条/秒
```

## 日志压缩服务

### 为什么需要独立服务？

在高并发环境下（WORKERS=4），如果在主服务中执行日志压缩：
- ❌ 4个Worker都会执行压缩任务
- ❌ 日志压缩重复执行4次
- ❌ 浪费CPU和磁盘I/O资源
- ❌ 影响API响应性能

使用独立服务后：
- ✅ 日志压缩只执行1次
- ✅ 主服务专注API处理
- ✅ 资源隔离，性能提升
- ✅ 可以独立监控和调试

### 查看日志压缩服务

```bash
# 查看服务状态
docker compose -f docker-compose.backend.yml ps log-compressor

# 查看实时日志
docker compose -f docker-compose.backend.yml logs -f log-compressor

# 重启服务（手动触发压缩）
docker compose -f docker-compose.backend.yml restart log-compressor
```

### 配置说明

在 `.env` 中：

```bash
# 日志压缩间隔（小时）
LOG_COMPRESS_INTERVAL_HOURS=2

# 启动时是否立即压缩
LOG_COMPRESS_ON_STARTUP=1

# 日志保留天数
LOG_RETENTION_DAYS=30

# 日志级别（高并发场景建议WARNING）
LOG_LEVEL=WARNING
```

## 监控和维护

### 查看服务状态

```bash
# 查看所有服务
docker compose -f docker-compose.backend.yml ps

# 查看资源使用
docker stats
```

### 查看日志

```bash
# Backend API
docker compose -f docker-compose.backend.yml logs -f backend-api

# Queue Worker
docker compose -f docker-compose.backend.yml logs -f queue-worker

# Log Compressor
docker compose -f docker-compose.backend.yml logs -f log-compressor

# 所有服务
docker compose -f docker-compose.backend.yml logs -f
```

### 重启服务

```bash
# 重启单个服务
docker compose -f docker-compose.backend.yml restart backend-api

# 重启所有服务
docker compose -f docker-compose.backend.yml restart

# 停止所有服务
docker compose -f docker-compose.backend.yml stop

# 启动所有服务
docker compose -f docker-compose.backend.yml up -d
```

### 更新代码

```bash
# 拉取最新代码
git pull origin main

# 重新构建并启动
bash force-rebuild-backend.sh
```

## 性能优化建议

### 1. 数据库优化

- ✅ 已启用读写分离（1主2从）
- ✅ 连接池配置合理（每容器50个连接）
- 建议：定期检查慢查询日志
- 建议：为常用查询添加索引

### 2. Redis优化

- ✅ 连接池配置合理（每容器200个连接）
- ✅ 队列批量处理（每次1000条）
- 建议：监控Redis内存使用
- 建议：定期清理过期键

### 3. 应用优化

- ✅ 使用异步I/O（FastAPI + Tortoise ORM）
- ✅ 日志级别设置为WARNING（减少I/O）
- ✅ 日志压缩独立运行（避免影响主服务）
- 建议：使用CDN加速静态资源
- 建议：启用HTTP/2

### 4. 系统优化

```bash
# 增加文件描述符限制
ulimit -n 65535

# 优化TCP参数
sysctl -w net.core.somaxconn=4096
sysctl -w net.ipv4.tcp_max_syn_backlog=4096
```

## 故障排查

### 问题1：服务无法启动

```bash
# 查看详细日志
docker compose -f docker-compose.backend.yml logs backend-api

# 常见原因：
# - 数据库连接失败：检查DB_HOST、DB_PASSWORD
# - Redis连接失败：检查REDIS_HOST、REDIS_PASSWORD
# - 端口被占用：检查6080端口是否被占用
```

### 问题2：性能不达预期

```bash
# 检查资源使用
docker stats

# 检查数据库连接数
# 登录MySQL执行：SHOW PROCESSLIST;

# 检查Redis连接数
# 登录Redis执行：CLIENT LIST

# 常见原因：
# - CPU不足：考虑增加容器数量
# - 内存不足：检查是否有内存泄漏
# - 数据库慢查询：检查慢查询日志
# - Redis性能瓶颈：考虑使用Redis集群
```

### 问题3：日志压缩服务异常

```bash
# 查看日志压缩服务日志
docker compose -f docker-compose.backend.yml logs log-compressor

# 重启服务
docker compose -f docker-compose.backend.yml restart log-compressor

# 常见原因：
# - 磁盘空间不足：清理旧日志或增加磁盘
# - 权限问题：检查logs目录权限
```

## 相关文档

- [日志压缩快速开始](./LOG_COMPRESSOR_QUICK_START.md)
- [日志压缩详细说明](./LOG_COMPRESSION_OPTIMIZATION.md)
- [日志压缩优化总结](./日志压缩优化总结.md)

## 技术支持

如有问题，请查看：
1. 项目文档目录
2. GitHub Issues
3. 日志文件（logs/目录）
