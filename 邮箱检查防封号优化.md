# 邮箱检查防封号优化

## 问题背景

原有的邮箱状态检查逻辑存在以下问题：

1. **顺序处理**：按照数据库查询顺序依次检查邮箱
2. **无延迟**：连续快速请求，容易被识别为机器人
3. **固定模式**：每次执行的顺序和时间间隔都相同

这些特征容易被 Microsoft 检测为自动化行为，增加封号风险。

## 优化方案

### 1. 打乱处理顺序

```python
# 🔥 防封号优化1: 打乱邮箱处理顺序
emails_list = list(emails)
random.shuffle(emails_list)
```

**效果**：每次执行时邮箱的检查顺序都不同，避免固定模式。

### 2. 添加随机延迟

```python
# 🔥 防封号优化2: 邮箱间随机延迟（模拟人工操作）
delay = random.uniform(email_check_delay_min, email_check_delay_max)
await asyncio.sleep(delay)
```

**效果**：模拟人工操作的不规律性，避免被识别为机器人。

### 3. 批次间延迟

```python
# 🔥 防封号优化3: 批次间更长的随机延迟
batch_delay = random.uniform(batch_delay_min, batch_delay_max)
await asyncio.sleep(batch_delay)
```

**效果**：避免短时间内大量请求，降低触发限流的风险。

## 配置说明

在 `.env` 文件中配置：

```bash
# ==========================================
# 邮箱检查配置（防封号）
# ==========================================
# 是否启用邮箱状态自动检查
ENABLE_EMAIL_CHECK=0

# 邮箱检查间隔（小时）
EMAIL_CHECK_INTERVAL_HOURS=1

# 邮箱间随机延迟（秒）- 模拟人工操作
EMAIL_CHECK_DELAY_MIN=2.0
EMAIL_CHECK_DELAY_MAX=5.0

# 批次间随机延迟（秒）- 避免频繁请求
EMAIL_BATCH_DELAY_MIN=5.0
EMAIL_BATCH_DELAY_MAX=10.0
```

### 配置建议

#### 保守策略（推荐）

适用于账号安全优先的场景：

```bash
ENABLE_EMAIL_CHECK=1
EMAIL_CHECK_INTERVAL_HOURS=2          # 每2小时检查一次
EMAIL_CHECK_DELAY_MIN=3.0             # 邮箱间延迟 3-8 秒
EMAIL_CHECK_DELAY_MAX=8.0
EMAIL_BATCH_DELAY_MIN=10.0            # 批次间延迟 10-20 秒
EMAIL_BATCH_DELAY_MAX=20.0
```

**特点**：
- ✅ 封号风险最低
- ⚠️ 检查速度较慢
- 适合：生产环境、重要账号

#### 平衡策略（默认）

适用于大多数场景：

```bash
ENABLE_EMAIL_CHECK=1
EMAIL_CHECK_INTERVAL_HOURS=1          # 每1小时检查一次
EMAIL_CHECK_DELAY_MIN=2.0             # 邮箱间延迟 2-5 秒
EMAIL_CHECK_DELAY_MAX=5.0
EMAIL_BATCH_DELAY_MIN=5.0             # 批次间延迟 5-10 秒
EMAIL_BATCH_DELAY_MAX=10.0
```

**特点**：
- ✅ 封号风险较低
- ✅ 检查速度适中
- 适合：日常使用、测试环境

#### 激进策略（不推荐）

仅用于紧急情况或测试：

```bash
ENABLE_EMAIL_CHECK=1
EMAIL_CHECK_INTERVAL_HOURS=0.5        # 每30分钟检查一次
EMAIL_CHECK_DELAY_MIN=1.0             # 邮箱间延迟 1-3 秒
EMAIL_CHECK_DELAY_MAX=3.0
EMAIL_BATCH_DELAY_MIN=2.0             # 批次间延迟 2-5 秒
EMAIL_BATCH_DELAY_MAX=5.0
```

**特点**：
- ⚠️ 封号风险较高
- ✅ 检查速度快
- 适合：紧急修复、临时测试

## 工作流程

### 优化前

```
查询第1批邮箱 (10个)
├── 检查邮箱1 ⏱️ 立即
├── 检查邮箱2 ⏱️ 立即
├── 检查邮箱3 ⏱️ 立即
└── ...

查询第2批邮箱 (10个)
├── 检查邮箱11 ⏱️ 立即
├── 检查邮箱12 ⏱️ 立即
└── ...

问题：
❌ 顺序固定（1→2→3→...）
❌ 无延迟，连续请求
❌ 容易被识别为机器人
```

### 优化后

```
查询第1批邮箱 (10个)
├── 打乱顺序 🔀 [3, 7, 1, 9, 2, ...]
├── 检查邮箱3 ⏱️ 延迟 3.2秒
├── 检查邮箱7 ⏱️ 延迟 4.8秒
├── 检查邮箱1 ⏱️ 延迟 2.5秒
└── ...
⏱️ 批次延迟 7.3秒

查询第2批邮箱 (10个)
├── 打乱顺序 🔀 [15, 11, 18, 13, ...]
├── 检查邮箱15 ⏱️ 延迟 4.1秒
├── 检查邮箱11 ⏱️ 延迟 2.9秒
└── ...

优势：
✅ 顺序随机，每次不同
✅ 随机延迟，模拟人工
✅ 批次间隔，避免限流
```

## 性能影响

### 时间成本

假设有 100 个邮箱需要检查：

**优化前**：
```
100个邮箱 × 0秒延迟 = 0秒
总耗时：约 100秒（仅API请求时间）
```

**优化后（默认配置）**：
```
邮箱间延迟：100个 × 平均3.5秒 = 350秒
批次间延迟：10批 × 平均7.5秒 = 75秒
API请求时间：约 100秒
总耗时：约 525秒（8.75分钟）
```

**优化后（保守配置）**：
```
邮箱间延迟：100个 × 平均5.5秒 = 550秒
批次间延迟：10批 × 平均15秒 = 150秒
API请求时间：约 100秒
总耗时：约 800秒（13.3分钟）
```

### 权衡建议

- **账号数量少（< 50个）**：使用保守策略，安全第一
- **账号数量中等（50-200个）**：使用平衡策略（默认）
- **账号数量多（> 200个）**：考虑分批执行，延长检查间隔

## 监控和调试

### 查看执行日志

```bash
# 查看邮箱检查日志
docker compose -f docker-compose.backend.yml logs -f backend-api | grep "邮箱"

# 或者查看 scheduler 日志
tail -f logs/scheduler/scheduler.log
```

### 日志示例

```
2026-02-22 10:00:00 - 开始检查邮箱状态
2026-02-22 10:00:00 - 防封号配置: 邮箱间延迟 2.0-5.0秒, 批次间延迟 5.0-10.0秒
2026-02-22 10:00:01 - 第 1 页获取到 10 个邮箱，开始检查...
2026-02-22 10:00:01 - 已打乱邮箱处理顺序（防止按固定顺序访问）
2026-02-22 10:00:02 - 检查邮箱 user3@example.com (1/10): has_ip=True, has_token=True
2026-02-22 10:00:05 - 随机延迟 3.2 秒（防止频繁请求）
2026-02-22 10:00:08 - 检查邮箱 user7@example.com (2/10): has_ip=True, has_token=True
...
2026-02-22 10:02:30 - 第 1 页处理完成，批次间延迟 7.3 秒...
```

### 调整策略

如果发现封号情况：

1. **增加延迟时间**：
   ```bash
   EMAIL_CHECK_DELAY_MIN=5.0
   EMAIL_CHECK_DELAY_MAX=10.0
   EMAIL_BATCH_DELAY_MIN=15.0
   EMAIL_BATCH_DELAY_MAX=30.0
   ```

2. **降低检查频率**：
   ```bash
   EMAIL_CHECK_INTERVAL_HOURS=3  # 改为每3小时
   ```

3. **减少批次大小**：
   修改代码中的 `batch_size = 10` 改为 `batch_size = 5`

## 最佳实践

### 1. 分时段执行

避免在高峰时段（工作时间）执行大量检查：

```bash
# 建议在凌晨或深夜执行
EMAIL_CHECK_INTERVAL_HOURS=24  # 每天执行一次
# 配合 cron 在凌晨2点执行
```

### 2. 监控异常率

定期检查日志，如果发现大量失败：

```bash
# 查看失败的邮箱
grep "检查邮箱.*失败" logs/scheduler/scheduler.log
```

### 3. 分批管理

对于大量邮箱，考虑分组管理：

- 重要账号：保守策略，低频检查
- 普通账号：平衡策略，正常检查
- 测试账号：激进策略，高频检查

### 4. 使用代理

如果有条件，配合代理使用：

- 不同邮箱使用不同IP
- 降低单IP请求频率
- 进一步降低封号风险

## 故障排查

### 问题1：检查速度太慢

**原因**：延迟配置过于保守

**解决**：
```bash
# 适当降低延迟
EMAIL_CHECK_DELAY_MIN=1.5
EMAIL_CHECK_DELAY_MAX=3.0
EMAIL_BATCH_DELAY_MIN=3.0
EMAIL_BATCH_DELAY_MAX=6.0
```

### 问题2：仍然出现封号

**原因**：延迟不够或其他因素

**解决**：
1. 增加延迟时间（见上文"调整策略"）
2. 降低检查频率
3. 检查是否有其他自动化操作
4. 考虑使用代理

### 问题3：定时任务未执行

**原因**：未启用邮箱检查

**解决**：
```bash
# 在 .env 中启用
ENABLE_EMAIL_CHECK=1
```

然后重启服务：
```bash
docker compose -f docker-compose.backend.yml restart backend-api
```

## 总结

通过以下三个优化：

1. ✅ **打乱顺序**：避免固定模式
2. ✅ **随机延迟**：模拟人工操作
3. ✅ **批次间隔**：避免频繁请求

显著降低了邮箱检查被识别为机器人的风险，提高了账号安全性。

建议根据实际情况调整配置，在安全性和效率之间找到平衡点。
