# Outlook Token 刷新状态更新

## 更新说明

在 Outlook 邮件收发功能中，当 Token 刷新失败时，系统会根据响应状态码自动更新邮箱状态。

## 更新内容

### 文件：`backend/app/clients/outlook.py`

修改了所有 HTTP 请求的状态码判断逻辑，将 408 超时错误加入重试机制：

**修改的方法：**
1. `get_token_from_url()` - 使用授权码换取 Token
2. `refresh_token()` - 刷新访问令牌（核心方法）
3. `get_user_info()` - 获取用户信息
4. `get_messages()` - 获取邮件列表
5. `move_message()` - 移动邮件
6. `send_message()` - 发送邮件

**统一的状态码处理逻辑：**
```python
# 5xx 服务器错误或 408 超时：触发重试
if status_code >= 500 or status_code == 408:
    raise Exception(f"请求失败: {status_code}")
elif status_code != 200:  # 或 201, 202，取决于接口
    logger.error(f"[{self.email}] ❌ 响应状态码: {status_code}")
    return 0
```

## 状态码处理逻辑

### 1. 5xx 服务器错误 + 408 超时（触发重试）
- **状态码**：500-599 和 408 Request Timeout
- **行为**：触发重试机制
- **重试次数**：最多 3 次（由 `@async_retry()` 装饰器控制）
- **失败后**：如果重试 3 次后仍失败，设置邮箱状态为 5
- **原因**：这些错误通常是临时性的，值得重试

### 2. 2xx 成功
- **状态码**：200-299
- **行为**：Token 刷新成功
- **状态**：不修改邮箱状态（保持正常）
- **操作**：更新 access_token 和 refresh_token

### 3. 其他状态码（3xx, 4xx 等，排除 408）
- **状态码**：300-399, 400-407, 409-499
- **行为**：立即设置邮箱状态为 2（异常）
- **原因**：这些状态码通常表示：
  - 401: Token 已失效或被撤销
  - 403: 权限不足
  - 400: 请求参数错误
  - 3xx: 重定向（不应该出现在 Token 刷新中）
  - 其他 4xx: 客户端错误，重试无意义

## 影响范围

此更新会影响以下操作：

### 1. 发送邮件 (`send_email_main`)
- 调用 `refresh_token()` 刷新 Token
- 如果刷新失败且状态码不是 2xx/5xx，邮箱状态设为 2
- **即时反馈**：用户发送邮件时立即知道邮箱异常

### 2. 接收邮件 (`get_emails_main`)
- 调用 `refresh_token()` 刷新 Token
- 如果刷新失败且状态码不是 2xx/5xx，邮箱状态设为 2
- **即时反馈**：用户接收邮件时立即知道邮箱异常

### 3. 邮箱状态检查 (`check_and_update_emails_logic`)
- 批量检查邮箱时，会调用 `get_emails_main`
- 间接触发 Token 刷新和状态更新
- **最终判断**：根据实际能否获取邮件来更新状态
- 注意：检测接口的状态更新会覆盖 `refresh_token()` 的状态设置

## 状态更新优先级

系统中有两个地方会更新邮箱状态，它们是**互补**的关系：

### 1. Token 刷新时更新（即时反馈）
- **位置**：`refresh_token()` 方法
- **触发时机**：发送/接收邮件时
- **更新条件**：Token 刷新响应状态码不是 2xx/5xx
- **优点**：快速反馈，用户立即知道问题
- **缺点**：可能不够准确（Token 刷新失败但邮箱可能还能用）

### 2. 批量检测时更新（准确判断）
- **位置**：`check_and_update_emails_logic()` 函数
- **触发时机**：手动触发批量检测或定时任务
- **更新条件**：实际测试能否获取邮件
- **优点**：更准确，基于实际功能测试
- **缺点**：不是实时的，需要等待检测任务

### 设计理念
- **即时反馈 + 准确判断**：两种更新机制互补
- Token 刷新失败时立即标记异常（快速）
- 批量检测时根据实际情况更新状态（准确）
- 如果 Token 刷新失败但后来恢复了，批量检测会把状态改回正常

## 邮箱状态说明

根据 `backend/app/models/mail.py` 中的定义：

```python
class Status(IntEnum):
    OK = 1   # 正常
    NOT = 2  # 异常
```

- **状态 1**：邮箱正常，可以正常收发邮件
- **状态 2**：邮箱异常，Token 刷新失败（非 5xx 错误）
- **状态 5**：邮箱异常，Token 刷新连续失败（5xx 错误重试 3 次后）

## 使用场景

### 场景 1：Token 被用户撤销
- 用户在 Microsoft 账户中撤销了应用授权
- Token 刷新返回 401 Unauthorized
- 系统自动将邮箱状态设为 2
- 管理员可以看到该邮箱异常，需要重新授权

### 场景 2：权限不足
- Token 权限范围不足
- Token 刷新返回 403 Forbidden
- 系统自动将邮箱状态设为 2

### 场景 3：服务器临时故障或超时
- Microsoft 服务器返回 503 Service Unavailable 或 408 Request Timeout
- 系统触发重试机制（最多 3 次）
- 如果 3 次都失败，设置状态为 5

## 测试建议

1. **正常场景**：验证 Token 正常刷新不会改变状态
2. **401 场景**：撤销授权后，验证状态是否变为 2
3. **5xx 场景**：模拟服务器错误，验证重试和状态 5
4. **批量检查**：验证批量检查邮箱时状态更新是否正确

## 日志示例

### 成功刷新
```
[user@example.com] Token 即将过期或已过期，正在刷新...
[user@example.com] ✅ Token 刷新成功！
```

### 非 2xx/408/5xx 失败
```
[user@example.com] Token 即将过期或已过期，正在刷新...
[user@example.com] ❌ 响应状态码: 401，设置邮箱状态为异常
```

### 408/5xx 重试失败
```
[user@example.com] Token 即将过期或已过期，正在刷新...
[user@example.com] 刷新 Token 连续3次失败，设置状态为5
```

## 注意事项

1. 状态更新是自动的，无需手动干预
2. 状态 2 的邮箱需要重新授权才能恢复
3. 状态 5 的邮箱可能是临时故障，可以稍后重试
4. 建议定期运行邮箱状态检查任务，及时发现异常邮箱
